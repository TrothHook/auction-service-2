AuctionsTableIAM:
  Effect: Allow
  Action:
    - dynamodb:PutItem
    - dynamodb:Scan
  Resource:
    - arn:aws:dynamodb:${self:custom.region}:825765414746:table/AuctionsTable-${self:provider.stage}
